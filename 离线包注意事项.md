# 离线包注意事项

## 1. URL 带时间戳但内容不变

有些站点会给静态资源追加时间戳 query，例如哈利法塔：

```text
https://www.burjkhalifa.ae/wp-content/themes/salient-child/assets/css/bootstrap.min.css?ver=1783339393
https://www.burjkhalifa.ae/wp-content/themes/salient-child/assets/css/bootstrap.min.css?ver=1783339665
```

这类 `ver` 看起来像版本号，但实测可能每次页面打开都会变化，资源内容没有变化。

如果离线包按完整 URL 精确命中，第一次导入的是 `?ver=1783339393`，下一次页面请求 `?ver=1783339665` 时就会 miss，导致离线包明明有资源但没有发挥作用。

## 2. 处理原则

不要一刀切去掉所有 query。只允许对明确的静态资源做稳定匹配。

可以做稳定匹配的条件：

1. 资源路径稳定，只有 query 变化。
2. 类型是静态资源：`.js`、`.css`、`.woff`、`.woff2`、`.ttf`、`.otf`。
3. query 只有一个 `ver=` 参数。
4. `ver` 值是纯数字或数字加点号。
5. 路径属于明确静态目录，例如 WordPress 的 `/wp-content/`、`/wp-includes/`。

不能做稳定匹配的资源：

1. HTML 主文档。
2. 接口请求。
3. 图片、视频、媒体流。
4. 带 token、session、签名、防盗链参数的 URL。
5. 带 `w/h/q/format/resize` 等转换参数的图片 URL。
6. 多 query 参数且语义不明确的 URL。

## 3. 推荐实现

端侧拦截时先按完整 URL 精确查离线包。

如果没命中，再对 WordPress 静态资源做一次“同路径匹配”：

```text
请求 URL: /assets/css/bootstrap.min.css?ver=1783339665
离线包 URL: /assets/css/bootstrap.min.css?ver=1783339393
稳定 key: /assets/css/bootstrap.min.css
```

只要稳定 key 相同，就可以返回离线包里的旧 `ver` 文件。

这样不需要后台每次因为时间戳变化重新导入，也不会把接口、图片裁剪参数等动态资源误缓存。

## 4. 后台导入注意

后台仍然可以记录真机发现的完整 URL，方便保留真实 MIME、大小、hash 和下载来源。

但判断是否需要重新构建时，不能只看 `ver` 参数变化。需要看：

1. 路径是否变化。
2. 文件内容 hash 是否变化。
3. 文件大小/MIME 是否异常。

如果只是 `ver` 变化、内容没变，不应该认为资源失效。

## 5. 哈利法塔当前结论

哈利法塔首页是 WordPress 静态资源，首屏主要资源在：

```text
/wp-content/themes/salient-child/assets/css/*.css?ver=...
/wp-content/themes/salient-child/assets/js/*.js?ver=...
/wp-includes/js/jquery/*.js?ver=...
```

真机日志中出现过：

```text
?ver=1783339393
?ver=1783339665
```

这说明 `ver` 是易变时间戳，不适合当作唯一离线包命中 key。

当前策略：

1. CSS/JS 进入离线包。
2. VWO、GTM、GA、Facebook、TikTok 等三方统计/实验脚本不进离线包，按站点拦截。
3. 端侧对 WordPress 静态资源启用同路径 fallback，解决时间戳变化导致的 miss。

## 6. 验证方式

真机验证时看 hilog：

```text
static REQ ...?ver=新时间戳
cache DECODE ...
cache HIT ...
WebPerf FCP ...
WebPerf onPageEnd ...
```

如果离线包已下载完成，下一次冷启动时即使 `ver` 变了，也应命中同路径离线资源。

## 7. 全量离线包下载验证

当前端侧策略是：服务端 `/api/config` 拉取成功后，所有 `bundle:true` 且有 manifest 的站点都会进入离线包下载队列。

注意边界：

1. 只有 `remoteConfig.getSource() === 'server'` 时才触发全量下载。
2. 本地旧配置只做兜底展示，不触发全量下载，避免旧 manifest 抢先下载。
3. 当前打开哪个网站不影响其它 `bundle:true` 站点离线包启动下载。
4. 首装或清数据后，沙箱会集中增长，需要控制后台总包大小。

验证方式：

```text
remote bundle: total x, to download y
cache STORE ... bundle ...
WebCache: ready, raw=0 disk=xxx bytes=xxxx
```

如果打开 A 网站后，日志中出现 B/C/D 多个站点域名的 `cache STORE ... bundle`，并且下一次启动 `disk` 数量和 `bytes` 明显增长，说明全量离线包已经开始下载。

临时抓取的 hilog、资源列表、过滤结果只用于分析，不应该提交到 git。
