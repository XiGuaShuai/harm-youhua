# 后台运维记录（2026-08-19 ~ 2026-08-21）

更新时间：2026-08-21（当前进度）

这份文档给下一次会话和真人运维用。先读完再改线上配置。之前几次出错，都是因为暂停、删除、打包装包、发版这几件事没有分开记清楚。

---

## 0. 当前进度（2026-08-21）

**现在只在测试环境做真机测试。预发/正式的包不要当成本轮验收对象，除非用户点名。**

后台入口：`https://maidun.chujingservice.com/app#/apps`（`/#/apps`、`/apps#/apps` 同一页）。顶部先切「测试 / 预发 / 正式」，再看「下发 / 暂停」。

| 应用 | 地区 | 测试 | 预发 | 正式 | 本轮结论 |
|---|---|---|---|---|---|
| 马来西亚电子入境卡 `app_mdac` | `100014` | **下发中**，30 个资源，只含首页 HTML `/mdac/main`，版本 `fd296b547368872f` | 下发中，29 个，无 HTML | 下发中，29 个，无 HTML（首页 HTML 未保住） | 注册页 `main?registerMain` **不要进包**。测试可测首页壳。名单曾被删，已从备份恢复 |
| FIFA `app_fifaworldcup` | 美/加/墨测试 ID | **下发中**，164 个，HTML 壳+主 CSS/JS，版本 `26d374fb9c771fb5` | 有旧包 162，不是本轮包 | 有旧包 162，不是本轮包 | 测试体感仍差：空壳 SPA + OneTrust/PingOne 同步脚本。禁止再打那两个 7MB+ 文件 |
| 俄罗斯 Galeria `app_galereya` | `100452` | 磁盘上已有 **55** 个资源（含当前首屏 HTML/CSS/JS），但 **测试/预发/正式都暂停**，公开配置 `bundle=false`，设备不会命中 | 暂停 | 暂停 | 要测测试包，必须先把测试拨回「下发」。Yandex/验证码不进包。Bitrix `?版本号` 会过期 |
| 威尼斯人 `app_venetianmacao` | `100009` 澳门 | **下发中**，31 个，版本 `6503a9091cffd616` | 名单无 | 名单无 | 测试已验证过命中。预发正式未上 |

真机宿主：`D:\Work\KCP\AtomicPlatform` 的 `start_net_test`（走 maidun `/test`）。不要提交 AtomicPlatform。设备约 60 秒拉一次配置，或重开应用。

未完成 / 等用户：

1. 用户说还有几个问题要讲，先听完再改。
2. Galeria 测试包要真机对比，须先「下发」测试。
3. FIFA 若还要更快，只能注入延后第三方脚本，不能靠继续堆包。
4. MDAC 正式首页 HTML 目前不在包里；若要和测试一样只留首页，再单独导正式。
5. 预发一律未按本轮新包验收。

相关入口：

- 线上后台：`https://maidun.chujingservice.com`
- 应用管理（用户常用）：`https://maidun.chujingservice.com/app#/apps`
- 应用管理（hash 标准地址）：`https://maidun.chujingservice.com/#/apps`
- 应用管理（兼容）：`https://maidun.chujingservice.com/apps#/apps`
- 离线包页：`https://maidun.chujingservice.com/#/bundles`
- 代码仓库：`D:\Work\KCP\离线包sdk\harm-youhua`
- 本次真机宿主：`D:\Work\KCP\AtomicPlatform`（`start_net_test`，不要提交）
- 线上容器：`youhua-admin` @ `47.236.73.89`
- 生效配置：容器内 `/app/server/data/config.json`（Docker 数据卷，不在 git）

不要把后台密码写进仓库或回复。

---

## 1. 先分清三件事：暂停、关离线包、删除应用

后台现在有三种完全不同的操作。搞混就会把应用弄没。

| 操作 | 在哪点 | 当前环境设备 | 服务器包文件 | 应用是否还在名单 | 恢复方式 |
|---|---|---|---|---|---|
| **下发 / 暂停** | 应用管理「离线包」列开关，或更多菜单 | 暂停后不再下载、不再命中 | **保留** | **还在**，开关显示「已暂停」 | 再拨回「下发」 |
| **取消勾选「离线包」** | 编辑页加速项 | 该应用从该环境公开配置消失 | 一般还在 | 应用还在总名单，但当前环境页可能看不到 | 编辑页重新勾选环境并保存 |
| **删除应用** | 更多 → 删除应用 | 三个环境都消失 | 包目录通常还在，但配置没了 | **名单里没有了** | 只能从 `config.json` 备份恢复 |

铁律：

1. 只是暂时不想用离线包，只拨 **下发 / 暂停**。不要点删除。
2. 暂停只作用于页面顶部当前选中的环境（测试 / 预发 / 正式）。三个环境要分别拨。
3. FIFA 这种地区应用是 **一份包给该环境下全部地区**。测试环境暂停 FIFA，美国/加拿大/墨西哥一起停，不是只关美国。
4. 删除应用不会自动删磁盘上的 `/bundles/<env>/<id>/`。看起来「包还在、名单没有」，就是这种。
5. 改线上配置前，先备份容器内 `config.json`。

暂停接口（后台已上线）：

```http
PUT /api/admin/apps/:id/bundle-pause
{ "environment": "test" | "pre" | "prod", "paused": true | false }
```

实现字段：`bundlePausedEnvironments`。暂停后公开配置里应用还在，但 `bundle=false`，端侧会按现有逻辑停用本地包。

---

## 2. 后台入口和发版

### 2.1 页面地址

管理界面是 hash 路由。真正的应用管理路由是 `#/apps`。

用户常用 `https://maidun.chujingservice.com/app#/apps`。`/app` 和 `/apps` 都会返回同一套 SPA，hash 为 `#/apps` 时就是应用管理。不要再把 `/app#/apps` 当成另一个系统。

2026-08-21 已把暂停开关发到 maidun。打开后强制刷新（Ctrl+F5）。顶部先切环境，再看每一行「下发 / 暂停」。

### 2.2 发版

后台代码更新：

```powershell
cd D:\Work\KCP\离线包sdk\harm-youhua
git push server HEAD:main
```

服务器收到后自动 `docker compose up -d --build`。数据卷保留，**不会**用 git 里的旧 `admin/server/data/config.json` 覆盖线上配置。

本次相关提交：

- `e323756` feat: 后台按环境暂停离线包下发，并兼容 /app#/apps 入口
- `be90e23` fix: 把地区目录打进镜像，修复 /app#/apps 构建

发版踩坑：`admin/web/src/views/Apps.vue` 依赖 `regionCatalog.js` → `server/region-catalog.js`。Docker web 构建阶段原来只 COPY `web/`，缺文件会构建失败。已在 `admin/Dockerfile` 增加：

```text
COPY server/region-catalog.js /server/region-catalog.js
```

以后改地区目录或应用管理页，这两个文件必须一起进 git。

---

## 3. 本次应用状态（以线上为准）

查公开配置：

```text
https://maidun.chujingservice.com/test/api/config
https://maidun.chujingservice.com/pre/api/config
https://maidun.chujingservice.com/prod/api/config
```

### 3.1 马来西亚电子入境卡 MDAC

| 项 | 值 |
|---|---|
| 应用 ID | `app_mdac` |
| 名称 | 马来西亚电子入境卡(MDAC) |
| URL | `https://imigresen-online.imi.gov.my/mdac/main` |
| 范围 | `region` |
| 地区 | `100014` 马来西亚（test/pre/prod 共用这个短 ID） |
| 离线包环境 | test、pre、prod 都配置了 |
| 测试包 | **30** 个 URL；只含首页 HTML `/mdac/main` + 首页 CSS/JS。版本 `fd296b547368872f` / 压缩 `9c78bec8afbda932` |
| 预发包 | 29 个 URL，不含 HTML。未按这次要求改 |
| 正式包 | **30** 个 URL；与测试一样只含首页 HTML `/mdac/main` + 首页 CSS/JS。版本 `1648c1f88cfa0d35` / 压缩 `b1e0cf12f231840b` |

**注册页不要进包。** `https://imigresen-online.imi.gov.my/mdac/main?registerMain` 已加入排除。图片、register 接口仍排除。首页 HTML 壳可以缓存，session/验证码走 JS 和网络。

当前实测：测试 30 含首页 HTML；正式/预发 29 不含 HTML。不要假设三套包一样。

**事故（2026-08-21）：**

测试环境马来西亚地区看不到 MDAC。核对结果：

- 公开 `test/pre/prod` 都没有 `app_mdac`
- 后台 `config.json` 里整条应用被删
- 不是暂停。暂停会留在名单里
- 磁盘上的包还在：`/test/bundles/app_mdac/manifest.json` 仍是 200，约 38 条
- 当天 `config.json` 写盘时间 `2026-08-21 02:10` UTC
- 同时名单里多了 `app_harbourcity`，并保留了后来加的 `app_macaosands`、`app_venetianmacao`

**恢复：**

从备份取出整条 `app_mdac` 插回当前配置，不覆盖其它应用。

```text
备份源：/app/server/data/config.json.bak.disable-harbourcity.1787045748575
恢复前再备：/app/server/data/config.json.bak.restore-mdac.1787278329810
插入位置：earrivalcard 后面
```

恢复后复验：

```text
test  FOUND bundle=true region=100014
pre   FOUND bundle=true region=100014
prod  FOUND bundle=true region=100014
```

以后发现某地区应用「不见了」，按这个顺序查：

1. 后台该环境页有没有这一行（没有 = 配置没了或 `bundleEnvironments` 不含该环境）
2. 开关是不是「暂停」（有行、已暂停 = 只是没下发）
3. `GET /{env}/api/config` 里有没有这个 `id`
4. `GET /{env}/bundles/{id}/manifest.json` 还在不在
5. 有备份就只把这一条插回去，**禁止**拿整份旧备份覆盖当前 `config.json`

### 3.2 FIFA World Cup 2026

| 项 | 值 |
|---|---|
| 测试应用 ID | `app_fifaworldcup` |
| 预发/正式应用 ID | `app_fifa`（正式业务编码；测试不要和这个混） |
| 入口 | `https://www.fifa.com/en/tournaments/mens/worldcup/canadamexicousa2026` |
| 测试地区 | 美国 `2037443812888760321`、加拿大 `2037443325779070977`、墨西哥 `2048685521584848897` |
| 本次范围 | **只动测试**。预发/正式未按这次要求开启新包 |
| 测试包现状 | 164 个资源，原始约 13.4MB，压缩约 7.8MB，版本 `26d374fb9c771fb5` / 压缩 `c5f108da23264afc` |

测试包里有：首页 HTML 壳、`smt-base-bridge`、主 CSS/JS、字体、既有 digitalhub/play.fifa 静态。

测试包里 **不要** 再放：

- OneTrust `OtAutoBlock.js`（约 7.9MB，同步脚本）
- PingOne `davinci.js`（约 7.3MB，同步脚本）
- Adobe Launch、Cookie 横幅、广告、Sentry、reCAPTCHA、bot-guard
- `cxm-api` 业务接口（后台 `bundleExcludeUrls` 有 `*api*`，JSON 接口会被清单丢掉）

原因：FIFA 首页是空壳 SPA。HTML/CSS/主 JS 命中后，解析器仍被头部两个超大同步脚本堵住，`main.js` 会晚约 4 秒才开始请求。把这两个文件打进包，包会膨胀十多 MB，真机还要执行 15MB JS，体感仍然差。用户明确说「还是慢，而且离线包太大」，已从测试包撤掉。

FIFA 剩下的慢，不是离线包没命中，是站点自己的同步第三方 + JS 执行 + `cxm-api`。要用注入延后脚本才能再抠首屏，且可能影响 Cookie 墙和登录。未做注入。

### 3.4 俄罗斯 Galeria

| 项 | 值 |
|---|---|
| 应用 ID | `app_galereya` |
| URL | `https://www.galeria.spb.ru/?isFirst=1` |
| 地区 | `100452` 俄罗斯 |
| 本次 | **只改测试** |

Bitrix 落地页。缓存键保留 `?` 版本号，站点一更新旧 CSS/JS 全部 MISS，首屏会打回俄罗斯源站。

2026-08-21 测试包已补当前首屏 6 项：首页 HTML、page/template CSS、kernel/template/page JS。包 55 个资源，版本 `16a9cdd11da72f52` / 压缩 `ccae9839093e4aa8`。Yandex/Mail.ru/Afisha/SmartCaptcha 不进包。预发正式未动。站点再发版后 query 会变，需要重新导入。

### 3.3 威尼斯人购物中心

本次更早已经在测试环境验证过离线命中。不要和 MDAC、FIFA 混改。预发/正式等用户明确说再动。

---

## 4. 打包装包时必须遵守

1. 用户没点名预发/正式，就只改测试。
2. 先看真机 `WAStats`：HIT 还是 `NETWORK_NOT_HANDLED`。命中了还慢，优先查同步脚本和接口，不要继续堆包。
3. 单个文件超过约 2MB、又不是本站首屏 CSS/JS，默认不进包。
4. Cookie / 登录 / 验证码 / 广告 / 统计 / 带 `latest` 的第三方 SDK，不进包。
5. `*api*` 在 FIFA 排除列表里。想进 `cxm-api` 必须先改排除规则，否则导入成功也会被清单丢掉。
6. 导入大文件用 `/api/admin/bundles/:id/import`（服务器去拉），不要用 `import-content` 一次 POST 十几 MB，会被 413。
7. 改完看公开配置的 `bundleVersion` 和 `progress=x/x`。设备 60 秒刷新一次配置。
8. 真机安装 HAP 必须 27 HSP + 1 HAP 成套，版本号一致。锁屏时 ability 可能没真正拉起。

---

## 5. 改线上配置的安全步骤

1. 先备份：

```bash
ssh -i ~/.ssh/youhua_deploy yuan@47.236.73.89
docker exec youhua-admin sh -c 'cp /app/server/data/config.json /app/server/data/config.json.bak.$(date +%s)'
```

2. 只改目标应用。优先用后台界面，或 `PUT /api/admin/apps/:id`。不要整表 `PUT /api/admin/apps` 漏应用。
3. 含中文名称不要用 curl/PowerShell 管道 PUT，会变成 `????`。
4. 改完立刻查：

```text
后台名单有没有这个 id
GET /test/api/config 有没有这个 id
GET /test/bundles/<id>/manifest.json 是不是 200
```

5. 用过的 `/tmp` 脚本和 JSON 不要当生效配置。

容器内已有备份（常用）：

```text
/app/server/data/config.json.bak.disable-harbourcity.1787045748575
/app/server/data/config.json.bak.restore-mdac.1787278329810
```

---

## 6. 新会话检查清单

打开新对话先做：

1. 读本文和 `新会话快速上下文.md`。
2. `curl` 三个环境的 `/api/config`，确认 `app_mdac`、`app_fifaworldcup` 还在测试里。
3. 不要假设本地 `admin/server/data/config.json` 等于线上。
4. 不要提交 `AtomicPlatform`。
5. 不要为了「再快点」把 OneTrust/PingOne 打进 FIFA 包。
6. 用户说某个地区应用不见了，先区分暂停、环境过滤、删除三条路径，再动手。
