# 后台运维 — 新增应用操作指南

> 给**运维/管理员**看的内部文档(不给 SDK 接入方)。记录在管理后台新增网页应用的操作步骤、配置经验、以及网页卡顿点分析。
> 线上后台:https://maidun.chujingservice.com/ (admin / 见服务器 .env 的 ADMIN_PASS)

---

## 一、新增一个应用的操作步骤

### 方式 A:管理界面操作(推荐)
1. 登录后台 → **应用管理** 页
2. 点「新增网页应用」
3. 填 URL → 点「**一键探测**」(自动带出名称/类型/建议加速参数)
4. 检查/调整参数(见下方配置说明) → 保存
5. 若开了离线包,保存后弹窗「立即构建」→ 点构建
6. 设备下次开机拉 `/api/config` 自动出现新应用

### 方式 B:直接换线上 config.json(脚本/批量时)
- **生效的 config.json 在线上服务器的 docker 数据卷里,不在 git 仓库** —— 仓库里 `admin/server/data/config.json` 是过时副本,别拿它当基准、别往它写。
- **永远先拉线上当前配置做基准**:`curl -s https://maidun.chujingservice.com/api/config`,在它基础上改出整份新配置。
- **⚠️ 含中文(应用名)必须 `docker cp` 整份文件替换,不能用 curl PUT** —— curl/PowerShell 传中文会乱码。
- 换文件步骤(`yuan` 读不了数据卷目录但能跑 docker,故走容器):
  ```bash
  # 1) 本地校验新配置是合法 JSON: node -e "require('./new.json')"
  # 2) 传上去
  scp -i ~/.ssh/youhua_deploy new.json yuan@47.236.73.89:/tmp/c.json
  # 3) 容器内先备份再替换(容器名 youhua-admin,后端读文件即生效,无需重启)
  ssh -i ~/.ssh/youhua_deploy yuan@47.236.73.89 "
    docker exec youhua-admin sh -c 'cp /app/server/data/config.json /app/server/data/config.json.bak.\$(date +%s)'
    docker cp /tmp/c.json youhua-admin:/app/server/data/config.json && rm -f /tmp/c.json"
  # 4) curl 公网 API 复验生效;本地临时文件删掉
  ```

---

## 二、应用配置字段说明

| 字段 | 作用 | 注意 |
|---|---|---|
| **`id`** | 应用唯一标识 | **必须用英文/ASCII!** 中文会导致离线包 URL(`/bundles/<id>/...`)Bad Request,离线包坏掉 |
| `name` | 显示名 | 可中文 |
| `url` | 入口 URL | — |
| `bundle` | 是否建离线包 | 见下方"哪些站适合离线包" |
| `swrDoc` | 主文档缓存(陈旧即用) | 一般 true |
| `prerender` | 离屏预渲染(秒开) | 重站慎用,见下 |
| `routes` | 二级页路径(预取用) | 如 `/shop/`、`/forms/xxx` |
| `extraBlockHosts` | 该应用专属黑名单 | 如某站的遥测域 |
| `prefetchChunks` | chunk 预取开关 | 一般跟随全局 |

---

## 三、按站点类型配置

### 1. 静态资源站(同源 JS/CSS 带 hash) — 适合完整离线包
- 特征:用一键探测能发现同源可缓资源(Next.js / 带内容hash的JS/CSS)
- 配置:`bundle:true + swrDoc:true + prerender:true`
- 在后台点「构建」建离线包,首屏明显变快。

### 2. 动态/反爬/视频站 — 离线包做不了
- 特征:一键探测发现的资源≈0(资源 JS 动态加载/跨域 CDN/反爬,cache-builder 只抓同源静态)
- 配置:`bundle:false + swrDoc:true + prerender:true`
- 靠预渲染 + 运行时缓存(边用边缓) + 拦遥测加速。

### 3. 重站(视频/大图/无限滚动流) — 预渲染排到列表后面
- 现象:重站离屏预渲染会占满渲染线程 → 卡屏。
- 解法:`prerender:true`(保留加速)+ **把该应用排到 apps 数组最后**。
- 原理:端侧 MAX_EAGER_PRERENDER=2,前 2 个立即预渲染,其余延迟。重站排后面 = 延迟预渲染,不抢首屏渲染线程。

### 4. 国际站(海外/跨境) — 快慢主要看网络节点
- 节点好→快,节点封→白屏。这不是框架问题,框架管不了跨境网络。用稳定的国际节点。

### 5. 按 UA 判断的站(默认 UA 返回桌面版/不适配 → 白屏)
- **已在 SDK 端解决**:SDK 自动以手机 UA 加载。对所有站通用,无需单独配。

---

## 四、网页卡顿/慢 的卡顿点分析(排查参考)

网页"加载慢/卡"可能是以下几类原因。**框架只能加速「能缓存的静态资源」,其余多为天花板。**

| 卡顿点 | 现象 | 框架能不能治 | 怎么判断 |
|---|---|---|---|
| **① 网络/节点慢** | 资源/数据拉不到、白屏、转圈 | ❌ 框架管不了(跨境/代理/节点) | 换节点就好 → 就是网络问题 |
| **② 数据接口慢/失败** | 框架(壳)出来了,内容不出来/报错 | ❌ 管不了(网站后端API) | 框架在、内容空 → 接口问题 |
| **③ 大图渲染** | 加载完后卡顿一下(渲染线程满) | ⚠️ 缓存能省下载,但解码渲染治不了 | 日志有大量大尺寸 Image event |
| **④ 预渲染抢渲染线程** | 进重站时卡(离屏建重Web) | ✅ 能(prerender 排最后/关) | 日志 RenderFrame task too long |
| **⑤ JS执行/解析慢** | JS密集站首次执行慢 | ❌ 框架改不了(内核+网站JS),元服务禁字节码注入 | 日志有 ArkCompiler/compile 告警 |
| **⑥ 静态资源没缓存** | 首次访问慢,第二次快 | ✅ 能(运行时缓存/离线包) | 第二次明显变快 |
| **⑦ 网站本身重** | 大图/视频/无限流/复杂DOM | ❌ 网站设计,框架不改页面 | — |

**关键认知:**
- **框架能加速的**:静态资源缓存/离线、预渲染、主文档SWR、拦遥测、手机UA。
- **框架管不了的**:网络节点、数据接口、视频流、网站本身的渲染/JS负担。
- 排查时**先抓日志区分**:RenderFrame 卡=渲染;ArkCompiler 告警=JS;Image event 大图=图片;net timeout=网络;框架在内容空=接口。

---

## 五、离线包管理(后台 Bundles 页)

- **检查更新**:看源站有没有变,只报告不下载
- **增量更新**:只补新增/缺失资源(省时)
- **强制重建**:清空重抓全部(大改/乱了时用)
- **生成清单**:只扫已有文件重建 manifest
- **自动更新**:每天一次自动 check→有更新就 update→失败 fallback build 重试(已开启,Bundles 页可看结果/手动触发)

### 离线包的三层自动维护(都已开启,无需手动盯)

源站改版/离线包对不上导致"新页面报错"时,有三层机制自动修复,**通常不用人工干预**:

| 层 | 触发 | 行为 | 时效 |
|---|---|---|---|
| **① 被动定时** | 每天一次(+启动后) | 遍历 bundle:true 站 check→变了 update→失败重建 | 最长 24h |
| **② 主动自愈** | 用户端真遇到离线包资源读不出 | 端侧上报后台 → 后台立即重建该站离线包 | 秒级,最及时 |
| **③ 用户兜底** | 离线包某资源坏/丢 | 端侧自动清脏索引、回退走网络 | 实时,用户不白屏 |

- **②主动自愈细节**:端侧只在"确属某 bundle 站离线包清单的资源实际读不出"时上报(普通运行时缓存被 LRU 清掉不报);后端同站 5 分钟防抖(大量上报只重建一次);动态站/未配离线包的站忽略。端点 `POST /api/report-error`(公开,端侧调)。
- **运维何时要手动管**:连续多天同一站自动重建仍报错 → 多半是源站资源结构大改/反爬升级,自动抓不全 → 去 Bundles 页**强制重建**,还不行就考虑把该站改 `bundle:false`(退回预渲染+运行时缓存)。

### 新增静态站:保存即自动构建离线包

后台 Apps 页保存一个 `bundle:true` 且服务器上还没离线包的站时,**后端会自动在后台构建一次离线包**(前端提示"后台构建中",约 30s 后刷新状态列)。所以加静态站不用再手动去 Bundles 页点构建,用户第一次进入即可从离线包加载(首屏即快)。

---

## 六、新增应用快速判断流程

```
拿到一个要加的网站
  ↓ 一键探测(后台填URL点探测)
  ├─ 探测到同源可缓资源 → bundle:true,建离线包
  └─ 动态/反爬/视频站(探测资源≈0) → bundle:false,靠预渲染+运行时缓存
  ↓
是重站(视频/大图)? → 排到 apps 列表最后(避免预渲染卡屏)
是国际站? → 提醒用稳定节点(快慢看网络)
id 一律用英文(中文坏离线包)
```
