# 新会话快速上下文

更新时间: 2026-07-02

这份文档给新会话快速接手用。先读这里，再看 `README.md`、`SDK_OFFLINE_BUNDLE.md`、`OFFLINE_BUNDLE_ARCHITECTURE.md`。

## 当前定版口径

1. 端侧 SDK 只做“后台离线包下载 + WebView 本地命中”。
2. 端侧不做运行时静态资源自动缓存，不根据用户访问过的资源自行写缓存。
3. 当前不做预渲染、不做 SWR 主文档缓存、不做 chunk 预取。
4. 后台负责筛选大资源/慢资源、生成离线包、托管 manifest 和资源文件。
5. 后台更新离线包后，端侧不会被实时推送；需要重新打开应用或调用 `WebAccel.refreshConfig()` 后才会拉新版本。
6. 服务端配置拉取成功后，所有 `bundle:true` 且有 manifest 的站点都会进入离线包下载队列；本地旧配置只兜底展示，不触发全量下载。
7. 离线包必须落到接入方应用沙箱，否则 WebView 无法本地命中；这是“后台离线包落盘”，不是“端侧运行时自动缓存”。

## 仓库结构

```text
harm-youhua/
├── app/
│   ├── webaccel/   # HAR SDK，核心逻辑
│   └── entry/      # 元服务 demo，本地真机安装用
├── admin/
│   ├── server/     # Node/Express 后台，配置 API + 离线包托管
│   └── web/        # Vue 后台管理界面
├── _产物交付/
│   └── webaccel-SDK交付包.zip
├── SDK_OFFLINE_BUNDLE.md
├── OFFLINE_BUNDLE_ARCHITECTURE.md
└── 项目简化时序图.md
```

## 关键端侧文件

1. `app/webaccel/src/main/ets/core/WebCacheManager.ets`
   - `sourceKind === 'bundle'` 才允许写入沙箱。
   - `flushPending()`、`seed()`、`download()` 已不再做运行时回填缓存。
   - 旧的非 `bundle` 来源沙箱记录会在拦截时移除，不再命中。
2. `app/webaccel/src/main/ets/WebAccelDebugBadge.ets`
   - 离线包状态显示为 `包准备中`、`包下载 x/y`、`包完成 x/y`。
   - 不再显示“缓 N 条”，避免误解成端侧运行时缓存。
3. `app/webaccel/src/main/ets/core/WebPreRender.ets`
   - 仍负责监听远程配置并触发 `loadBundleFor()`。
   - 只有服务端配置到达后才会对全部 `bundle:true` 站点启动离线包下载。
   - 预渲染/SWR/chunk 预取在当前口径下不作为交付能力使用。

## 线上后台

线上后台地址:

```text
https://maidun.chujingservice.com
```

注意:

1. 线上有效配置在服务器/Docker 运行环境中，不要只改本地 `admin/server/data/config.json`。
2. 后台凭据在仓库外部文档 `D:\Work\KCP\harm-youhua后台凭据.md`，不要把密码写进仓库或回复里。
3. PowerShell/Node 管道处理中文时容易把应用名写成 `????`。改中文字段时优先用 UTF-8 文件、Unicode 转义或后台界面，保存后用 `/api/config` 验证。

## 新加坡环球影城当前状态

应用 ID:

```text
rwsentosa
```

入口 URL:

```text
https://www.rwsentosa.com/zh-cn/play/universal-studios-singapore
```

UA:

```text
Mozilla/5.0 (Linux; U; Android 14.0; zh-cn; Xiaomi 13 Pro Build/UPB1.230309.014) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/119.0.0.0 Mobile Safari/537.36 XiaoMi/MiuiBrowser/14.7.10
```

后台配置:

```text
bundle=true
staticCache.enabled=false
swrDoc=false
prerender=false
prefetchChunks=false
bundleExtraUrls=27 条
bundleMaxSizeKB=12288
```

离线包结果:

```text
资源数: 27
原始大小: 约 11265.9 KB
压缩/端侧落盘: 约 7116.3 KB
主要资源: JS、CSS、字体
不包含: MP4 视频、动态接口、埋点广告
```

字体已经进离线包，包括 `noto-serif-sc`、`noto-sans-sc`、`RWSBody`、`RWSHeader`、`AlegreyaSans`、`AlegreyaSansSC`、`rws-icons` 等。

页面慢的判断:

1. 后台构建耗时是“服务器到 RWS 源站/CDN”的下载耗时。
2. 真机慢是“手机下载离线包 + 解压 + WebView 执行 JS + 字体排版 + 图片/视频渲染”的综合耗时。
3. 真机日志里大字体可能仍有 1-3 秒级下载；即使资源进离线包，首次安装后的第一次也要先下载到沙箱。
4. RWS 的完整 load 慢还和 MP4、图片、第三方脚本、页面 JS 执行有关，不是单个后台下载文件超过 3 秒才会慢。

## 真机与构建

HDC:

```text
D:\Huawei\DevEco Studio\sdk\default\openharmony\toolchains\hdc.exe
```

当前设备:

```text
2NX0123C29000677
```

Bundle name:

```text
com.atomicservice.6917596829912522753
```

构建命令:

```powershell
cd D:\Work\KCP\harm-youhua\app
$env:DEVECO_SDK_HOME='D:\Huawei\DevEco Studio\sdk'
& "D:\Huawei\DevEco Studio\tools\hvigor\bin\hvigorw.bat" assembleHar -p module=webaccel@default
& "D:\Huawei\DevEco Studio\tools\hvigor\bin\hvigorw.bat" --mode module -p module=entry@default -p product=default assembleHap
```

安装命令:

```powershell
$hdc='D:\Huawei\DevEco Studio\sdk\default\openharmony\toolchains\hdc.exe'
$bundle='com.atomicservice.6917596829912522753'
$hap='D:\Work\KCP\harm-youhua\app\entry\build\default\outputs\default\entry-default-signed.hap'
& $hdc shell hilog -r
& $hdc uninstall $bundle
& $hdc install -r $hap
& $hdc shell aa start -b $bundle -a EntryAbility
```

看日志:

```powershell
$hdc='D:\Huawei\DevEco Studio\sdk\default\openharmony\toolchains\hdc.exe'
& $hdc shell hilog -x -T WebCache,RemoteConfig,WebAccel,WebPreRender
```

判断来源:

```text
cache STORE ... bundle      # 后台离线包来源，正确
cache STORE ... runtime     # 当前不应再出现
```

## SDK 交付包

当前交付压缩包:

```text
D:\Work\KCP\harm-youhua\_产物交付\webaccel-SDK交付包.zip
```

内容:

```text
webaccel.har
接入指南.md
离线包说明.md
导出API清单.ets
项目简化时序图.md
```

如果改了 SDK 代码，需要重新构建 HAR，并覆盖交付目录里的 `webaccel.har` 后重新压缩。

## 常见坑

1. 手机显示 `????`：后台中文 name 被 Windows 控制台编码写坏了，重新用 UTF-8 或 Unicode 转义保存配置。
2. 显示 `包准备中`：配置是 `bundle:true`，但 SDK 暂未拿到 manifest 进度。
3. 显示 `包下载 x/y`：离线包正在下载，首次打开仍可能慢。
4. 显示 `包完成 x/y`：离线包资源已落沙箱，后续同 URL 请求应本地命中。
5. 后台更新离线包后，端侧不实时强推。要重新打开应用或调用 `WebAccel.refreshConfig()`。
6. 重装、清应用数据、系统清沙箱都会让本地离线包消失，下次 SDK 初始化会重新下载。
7. 不要把 MP4、大视频、动态接口、埋点广告放进离线包。
