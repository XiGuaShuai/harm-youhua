# 网页页面形式与加速策略说明

> 本文先讲清楚「网页有哪几种页面形式」(静态页 / SSR / CSR / SPA…)——通用概念,看完任何网站都能自己判别;
> 再接上「在 harm-youhua 加速框架里,每一类页面能开哪几层加速、为什么」——结论全部以 SDK / 后台代码为依据。
> 适合想彻底搞懂「这个站属于哪类、该怎么加速」的人仔细看。
> 更新:2026-06-30

---

## 〇、一句话先记住

> **判断一个页面属于哪类,只看一件事:服务器发来的「第一个 HTML 文件」里,有没有现成的正文内容。**

- 有内容、而且是提前做好不变的 → **静态页**
- 有内容、但每次请求由服务器临时拼 → **SSR(服务端渲染)**
- 没内容、是个空壳,内容全靠浏览器跑 JS 现画 → **CSR(客户端渲染)**

其它名词(SPA、同构、ISR…)都是在这三种基础上的组织方式或组合,后面会逐个点到。

---

## 一、四种页面形式(通用概念)

### 1. 静态页(Static / 纯 HTML)

**是什么**:网页内容**提前写死在 HTML 文件里**,存在服务器上,谁来访问都发同一份成品。改内容要重新发布。

```html
<!-- 服务器发来的原始 HTML 就长这样,内容直接在里面 -->
<body>
  <h1>印尼电子入境卡</h1>
  <form>
    <label>护照号</label><input ...>
    ...完整表单都在...
  </form>
</body>
```

**比喻**:厨房提前做好一大盘标准餐,你来了直接端给你,人人同一份。

**典型**:政府表单页、产品说明 / 文档站、营销落地页、博客静态站。

**特点**:服务器最省力(只发文件)、首屏最快、内容不个性化。

---

### 2. SSR(Server-Side Rendering,服务端渲染)

**是什么**:你请求页面时,**服务器实时**去查数据库 / 看你是谁 / 算个性化,**在服务器上把内容拼成完整 HTML**,再发给你。发到你手里时已经是带内容的成品,但这份成品是**临时为你这次请求拼的**。

```html
<!-- 服务器现查现拼,每次可能不一样(搜索结果、价格、个性化) -->
<body>
  <h1>Booking — 巴黎酒店搜索结果</h1>
  <div>香格里拉 ¥2800/晚</div>   <!-- 服务器查了数据库才有 -->
  <div>四季酒店 ¥3500/晚</div>
</body>
```

**比喻**:你点单后厨房**现炒**,炒好装盘端上来。每个人点的不一样,但端上来都是成品。

**典型**:Booking 搜索结果、电商商品详情、个性化首页;技术上常用 Next.js、Nuxt 实现。

**特点**:HTML 自带首屏内容(对 SEO 和首屏友好),但**服务器要现拼,累且慢**——慢就慢在「服务器拼这一下」(即 TTFB,首字节时间)。

---

### 3. CSR(Client-Side Rendering,客户端渲染)

**是什么**:服务器只发一个**几乎空的 HTML 壳** + 一堆 JS。内容**全靠你的浏览器**下载 JS、跑起来、再去请求数据、再一点点画出来。

```html
<!-- 服务器发来基本是空的,什么内容都没有 -->
<body>
  <div id="root"></div>             <!-- 空壳!正文一个字都没有 -->
  <script src="/app.4f3a9b.js"></script>  <!-- 全靠它在你手机上现画 -->
</body>
```

**比喻**:服务器给你**一袋生鸡丁 + 调料 + 一张菜谱**,你自己(浏览器)在桌上现炒。炒得快慢看你这张桌子(手机性能)和领料速度(网络)。

**典型**:bilibili、YouTube、TikTok、绝大多数后台管理系统;技术上常用 React / Vue + Vite / webpack。

**特点**:首屏前必须「下载 JS → 执行 → 拉数据 → 渲染」,**首屏天然慢、白屏久**;但交互流畅,适合 App 式体验。

---

### 4. SPA(Single Page Application,单页应用)—— 不是第四种渲染,而是一种「组织方式」

**是什么**:整个网站只有**一个 HTML 页面**,在站内点来点去(首页→详情→设置)**不刷新整页**、不重新加载,而是靠前端 JS「路由」把局部内容换掉。

- SPA 的首屏**通常是 CSR**(空壳 + JS),所以前面把它和 CSR 归一类。
- 它强调的是「翻页不刷新」这个**交互特征**,不是「首屏怎么渲染」。
- 反义词是 MPA(多页应用):每点一个链接都向服务器要一个新 HTML 页(传统网站、静态站多是这样)。

**一句话**:CSR 讲的是「首屏内容哪来的(浏览器现画)」,SPA 讲的是「翻页要不要刷新整页(不刷新)」。一个站可以「首屏 SSR + 之后 SPA」,见下。

---

### 5. 混合 / 同构(Hybrid / Isomorphic / ISR)—— 现代大站基本都是这种

纯静态、纯 SSR、纯 CSR 在现实中越来越少。主流框架(Next.js / Nuxt)默认是**混合**:

- **首屏**用 SSR(或静态预渲染)发一份**带内容的 HTML**,保证首屏快、能被搜索引擎抓到;
- 浏览器加载完 JS 后**接管页面**(这一步叫 hydration / 注水),之后的翻页就变成 **SPA**(不刷新整页);
- ISR(增量静态再生):静态页 + 定时在后台重新生成,兼顾静态的快和内容的新。

**一句话**:首屏当 SSR/静态吃(快、有内容),之后当 SPA 用(交互顺)。Next.js 站大多是这个形态。

---

## 二、怎么自己判别一个站是哪类(三招)

### 招 1:看「网页源代码」(最准)

浏览器里**右键 → 查看网页源代码**(View Source)。注意:看的是**服务器发来的原始 HTML**,不是开发者工具 Elements 面板(那是 JS 跑完后的结果,会骗你)。

- 源代码里**能搜到正文文字**(标题、段落、商品名)→ **静态页 或 SSR**
- 源代码里基本是 `<div id="root"></div>` + 一堆 `<script>`,正文搜不到 → **CSR**

### 招 2:区分「静态页」还是「SSR」

两者源代码里都有内容,区别在于**内容是死的还是临时拼的**:

- 多刷新几次、换个账号 / 换个搜索词,内容**会变**(搜索结果、个性化、实时价格)→ **SSR**
- 内容**永远一样**,人人相同 → **静态页**

### 招 3:本框架的自动判别(`detectSite`)

后台「新增应用」有个**一键探测**,抓首页 HTML 自动判型,判据就是代码里这几条特征串:

| 探测到的标志(HTML 里出现) | 判定类型 | 含义 |
|---|---|---|
| `__NEXT_DATA__` 或 `/_next/static/` | `next` | Next.js 站(通常 SSR / 同构) |
| `type="module"` + `/assets/` 或 `/@vite/` | `vite` | Vite 构建(通常 CSR / SPA) |
| `/static/js/main.<hash>.js` | `cra` | Create React App(CSR / SPA) |
| 以上都没有 | `other` | 静态页或其它 |

> 代码位置:`admin/server/cache-builder.js` 的 `detectSite()`。它同时还统计「同源可缓资源数」和「跨域 CDN 域」,据此**自动建议该开哪几层加速**(下一节)。

---

## 三、在 harm-youhua 框架里,各类页面能开哪几层加速

⚠️ 关键认知(很多人会搞错):

> **「能不能加速、能开哪几层」不是看页面动不动、是不是 SSR,而是看两件具体的事:**
> 1. **主文档 HTML 里有没有内容**(决定 `swrDoc` 主文档缓存值不值得开);
> 2. **关键资源(JS/CSS/字体)是不是「同源 + hash/版本号命名」**(决定 `bundle` 离线包能不能做)。

加速一共 6 层(详见《加速方案说明.md》):离屏预渲染、离线包、主文档 SWR、路由预取、黑名单拦遥测、运行时缓存/chunk 预取。下面看每类页面适配哪些。

### 各类页面 × 加速层 对照表

| | 静态页 | SSR(含 Next 同构) | CSR / SPA(空壳) |
|---|---|---|---|
| **离屏预渲染** prerender | ✅ | ✅ | ✅(照样省白屏) |
| **主文档 SWR** swrDoc | ✅ 主文档有料,重开秒显 | ✅ 有首屏内容,可缓 | ❌ **空壳别缓**(缓了糊脸) |
| **离线包** bundle | ✅ 同源静态资源多 | 看资源:同源 hash✅ / 跨域 CDN❌ | 多 ❌(资源跨域 / 运行时现拼 / 反爬) |
| **codeCache + chunk 预取** | — | ✅ 仅 Next/webpack 站 | — |
| **路由预取** routes | 可选 | ✅ | 可选(只缓二级页主文档) |
| **黑名单拦遥测** | ✅ 通用 | ✅ 通用 | ✅ 通用 |
| **运行时缓存** | ✅ | ✅(SSR 跨域资源主力靠它) | ✅ **主力**(空壳站全靠它 + 预连接 CDN) |
| **首屏快慢的天花板** | 最快 | 卡在「服务器拼页面 + 跨境」 | 卡在「下 JS + 执行 + 拉数据」 |

### 逐类说明

#### A. 静态页 —— 加速收益最大,全开
- 主文档有完整内容 → `swrDoc` 开,重开直接秒显旧版、后台校验换新。
- 同源静态资源多 → `bundle` 离线包能做,关键 JS/CSS 本地命中。
- **实测**:印尼电子入境卡首屏 **136ms**(离线包命中),是所有站里最快的。

#### B. SSR / Next 同构 —— 能缓主文档,但天花板在服务器
- `swrDoc` + `prerender` 都开;Next/webpack 站额外开 `codeCache`(JS 字节码缓存)+ `prefetchChunks`(预取后续页面 chunk),代码里这两条**只对 Next/webpack 生效**。
- **离线包看资源在哪**:资源若在跨域 CDN(如 Booking 的 `bstatic.com`)→ 做不了,改用运行时缓存兜底。
- **天花板**:SSR 慢在「服务器实时拼页面」那一下(TTFB),这是网站后端的事,端侧框架治不了。Booking 首屏 ~1100ms 就卡在 SSR + 跨境。

#### C. CSR / SPA 空壳 —— 预渲染省白屏,但主文档别缓
- **`swrDoc` 必须关**:主文档是空壳,缓个空壳没意义,还可能把空白糊到首屏。代码里 bilibili 这类就配 `swrDoc: false`(见 `WebPreRender.ets` 注释)。
- `prerender` 照开:提前在屏幕外把 JS 跑起来、渲染好,点开就挂上来,**白屏照样省掉**。
- **离线包多半做不了**,原因三选一:① 资源全在跨域 CDN(YouTube/foodpanda)② 资源 URL 是 JS 运行时现拼的、抓 HTML 扫不到(YouTube)③ 反爬,后台抓站 0 字节(TikTok)。
- **主力靠运行时缓存 + 预连接 CDN 域**(`preconnectHosts`,开机就对资源所在的跨域 CDN 做 DNS+TLS 握手,压短白屏)。

---

## 四、几个真实站点归类(本项目)

| 站 | 页面形式 | swrDoc | 离线包 | 为什么 | 首屏实测 |
|---|---|---|---|---|---|
| 印尼电子入境卡 | 偏静态 | ✅ | ✅ 38 资源 1.8MB | 同源 hash 资源多 | **136ms** |
| K11 | 动态站(同构) | ✅ | ✅ 4 资源 1.6MB | 同源、带版本号 query | ~410ms |
| Booking | SSR + 重反爬 | ✅ | ❌ | 资源全在跨域 `bstatic.com` CDN | ~1100ms(天花板) |
| bilibili | CSR / SPA | ❌ | ❌ | 空壳主文档 + 资源跨域 | 靠预渲染省白屏 |
| YouTube(m.) | CSR / SPA | ❌ | ❌ | JS 运行时现拼资源 URL | 运行时缓存 + 预连接 |
| TikTok | CSR / SPA | ❌ | ❌ | 反爬,后台抓站 0 字节 | 运行时缓存 + 预连接 |

> 注意:K11 是**动态站**,照样做了离线包——再次印证「能否做离线包看资源、不看页面动不动」。

---

## 五、常见误区澄清

1. **「离线包是不是把整个页面存下来?」**
   不是。离线包打的是**一个个独立的静态资源文件**(JS/CSS/字体),不是主文档 HTML。命中是靠端侧拦截器逐个请求比对,本地有副本就用本地的应答——**不是替换整个页面**。

2. **「动态页面是不是就没法加速?」**
   能。动态(CSR/SSR)站照样能开预渲染省白屏、运行时缓存、预连接、拦遥测。只是**主文档缓存(swrDoc)要按内容性质判断**,离线包**看资源满不满足条件**。

3. **「SSR 不也算静态页吗?为什么做不了离线包?」**
   SSR 的**主文档是服务器临时拼的、每次可能不同**,不是静态文件,所以主文档靠 SWR 不靠离线包。而它做不了离线包的真正原因往往是**资源放在跨域 CDN / 运行时现拼 / 反爬**——跟「它是 SSR」没直接关系,是踩了资源的坑。

4. **「全缓下来沙箱会不会爆?」**
   不会。运行时缓存有**硬上限 64MB + LRU 自动淘汰**(缓满就删最久没用的),且只放行 hash/版本号命名的资源(动态数据、带尺寸参数的图片一律不缓)。实测真机占用约 16MB。详见缓存机制相关说明。

---

## 六、配套文档

- 《加速方案说明.md》—— 6 层加速分别是什么、整体架构、实测数据
- 《后台运维-新增应用指南.md》—— 新增一个站怎么探测、配开关、上线
- `admin/server/cache-builder.js` —— `detectSite()` 判型 + 加速建议的源码
- `app/webaccel/src/main/ets/core/WebPreRender.ets` —— 离屏预渲染池实现
- `app/webaccel/src/main/ets/core/WebCacheManager.ets` —— 离线包下载 / 拦截命中 / 64MB+LRU
