# 后台运维记录（2026-09-04）

更新时间：2026-09-04 12:04（Asia/Shanghai）

## 1. 本轮目标

测试、预发、生产三套环境中，所有已经配置 `configJsonSync` 的应用必须每 60 秒检查一次业务侧 `configJson`。三套环境分别读取自己的业务 appId 和配置接口，禁止跨环境覆盖。

账号密码属于敏感信息，不写入 Git。当前凭据只保存在服务器受控配置和工作区仓库外的本地敏感文档中。

## 2. 定时任务实现

线上 `youhua-admin` 与当前源码均使用：

```text
CONFIG_JSON_SYNC_INTERVAL_MS = 60 * 1000
```

服务启动时先同步一次，之后 `setInterval` 每 60 秒调用 `runIndependentConfigJsonSync('every-minute')`。任务按 `appId + environment` 隔离；单个目标失败不阻塞其他应用。

每分钟会执行检查，但只有源 `configJson` 内容变化时才写入配置和更新 `configJsonSyncedAt`。因此日志中 `ok=true, changed=false` 表示检查成功且内容没有变化，不是任务没运行。

## 3. 2026-09-04 故障与处理

现象：运营后台已修改 Dubai Hills Mall 测试 URL，离线加速后台两天没有更新。

根因：服务器容器把测试运营管理域名解析到不可达的私网地址，33 个测试同步目标每分钟都执行，但全部返回 `fetch failed`。预发和生产使用各自可访问的环境配置接口，当时均正常。

处理：

- 更新三环境同步凭据；凭据值不进入仓库。
- 测试环境 33 个目标切换到服务器可访问的测试配置读取接口。
- 预发 26 个、生产 26 个目标继续使用各自原有环境接口。
- 三环境现有 `configJson` 在修改同步规则时均保持不变，由后续定时任务按源内容更新。
- 后台应用编辑页文案从“每天 12:00 自动同步”改为“每 60 秒自动同步”。

服务器配置恢复点：

```text
/app/server/data/credential-backups/config-before-all-env-minute-sync-2026-09-04T04-01-30-748Z.json
```

服务器静态页面恢复点：

```text
/app/server/data/credential-backups/Apps-before-minute-label-2026-09-04T04-07-40-234Z.js
```

## 4. 验证结果

北京时间 12:02 的真实定时轮询：

| 环境 | 同步目标 | 成功 | 失败 | 内容变化 |
| --- | ---: | ---: | ---: | ---: |
| test | 33 | 33 | 0 | 33 |
| pre | 26 | 26 | 0 | 0 |
| prod | 26 | 26 | 0 | 0 |

北京时间 12:04 的后续定时轮询：

| 环境 | 同步目标 | 成功 | 失败 | 内容变化 |
| --- | ---: | ---: | ---: | ---: |
| test | 33 | 33 | 0 | 0 |
| pre | 26 | 26 | 0 | 0 |
| prod | 26 | 26 | 0 | 0 |

第二轮全部 `changed=0`，说明三套源内容没有继续变化，同时证明每分钟检查链路持续正常。

Dubai Hills Mall 验证：

- 应用 ID：`app_dubaihillsmall`
- 测试 `configJson.url` 已自动更新为 `https://www.baidu.com`
- 测试同步时间：`2026-09-04T04:02:28.616Z`
- 预发、生产仍保持各自业务配置中的原 URL，没有被测试配置覆盖

## 5. 运维检查

公开配置：

```text
GET https://maidun.chujingservice.com/test/api/config
GET https://maidun.chujingservice.com/pre/api/config
GET https://maidun.chujingservice.com/prod/api/config
```

后台同步日志：

```text
GET /api/admin/config-json-sync/log
```

判断标准：

- 每隔约 60 秒出现新的 `trigger=every-minute` 记录。
- 各环境 `ok` 数应等于已配置同步目标数，`failed` 应为 0。
- `changed=0` 可以是正常结果；只有 `ok=false` 才是同步失败。
- 修改某环境业务配置后，只验证同环境公开配置，避免把测试 URL 当成预发或生产应有值。
