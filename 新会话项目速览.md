# harm-youhua 新会话项目速览

> 更新时间：2026-08-17  
> 基准分支：`feature/auto-update-and-optimize`  
> 阅读代码时的基准提交：`857b2f4 feat: isolate WebAccel config environments and refresh lifecycle`

这份文档用于让新会话快速建立正确上下文。先读父目录 `D:\Work\KCP\离线包sdk\当前实施状态.md` 和 `工作区使用约定.md`；本文补充代码结构。旧文档与这里冲突时，以父目录当前状态、本文和实际代码为准。

## 1. 一句话说明

本项目由两部分组成：

1. `app/webaccel`：鸿蒙 ArkWeb 离线资源加速 SDK，产物是 HAR。
2. `admin`：Node/Express + Vue 后台，负责应用配置、离线包构建、压缩、托管和更新。

核心链路只有一条：

```text
后台配置应用和静态资源
  -> 生成 manifest.json / manifest.zz.json
  -> SDK 拉取 /api/config
  -> 按 top / region / 按需规则下载离线包到宿主应用沙箱
  -> WebView 请求原站 URL
  -> SDK 用原站 URL 命中本地文件
  -> 未命中则放行网络
```

## 2. 当前定版边界

端侧 SDK 当前负责：

- 拉取后台 `/api/config`，并缓存最近一次成功配置。
- 下载后台已经选好的离线包资源。
- 将资源保存到接入方自己的 `filesDir/webcache` 沙箱。
- 在 ArkWeb 请求阶段按原站 URL 返回本地资源。
- 支持原始资源和 zlib `.zz` 压缩资源。
- 支持 TOP 常驻包、地区包、页面按需加载、版本更新、LRU 容量控制。
- 支持按 test/pre/prod 拉取、保存和更新独立的 `configJson`，供宿主业务接口失败时兜底。

当前明确不做：

- 不做用户访问后的运行时静态资源自动缓存。
- 不做主文档 SWR。
- 不做离屏预渲染。
- 不做 chunk 预取。
- 不把离线包打进 HAP/rawfile。
- 不做后台到端侧的实时强推。

旧字段 `swrDoc`、`prerender`、`prefetchChunks`、`staticCache` 仍可能出现在接口、后台配置或历史代码中，主要用于兼容或后台筛选；不能据此认为端侧已经恢复这些交付能力。

## 3. 仓库地图

```text
harm-youhua/
├── app/
│   ├── webaccel/       # HAR SDK，端侧核心
│   └── entry/          # 元服务 Demo/真机验证壳
├── admin/
│   ├── server/         # Express API、离线包构建与托管
│   └── web/            # Vue3 管理后台
├── deploy/             # nginx/服务器部署脚本
├── _产物交付/           # SDK 对外交付压缩包
├── docker-compose.yml  # admin + MySQL
└── 本文及其他设计/运维文档
```

### 推荐阅读顺序

1. 本文。
2. [`app/webaccel/README.md`](app/webaccel/README.md)：当前 SDK API 和地区缓存模型。
3. [`SDK_OFFLINE_BUNDLE.md`](SDK_OFFLINE_BUNDLE.md)：离线包边界和资源选择原则。
4. [`OFFLINE_BUNDLE_ARCHITECTURE.md`](OFFLINE_BUNDLE_ARCHITECTURE.md)：端、云、沙箱的总体关系。
5. [`admin/README.md`](admin/README.md)：后台能力和接口。
6. [`CLAUDE.md`](CLAUDE.md)：历史项目导航、构建和运维约束。
7. [`TROUBLESHOOTING.md`](TROUBLESHOOTING.md)：只作为历史踩坑记录，不能直接当当前能力说明。

## 4. 端侧 SDK 关键代码

### 4.1 门面与初始化

[`app/webaccel/src/main/ets/WebAccel.ets`](app/webaccel/src/main/ets/WebAccel.ets)

`WebAccel.init()` 的顺序是：

1. 提前初始化 ArkWeb 内核。
2. 初始化 `filesDir/webcache`、磁盘索引和 bundle 状态。
3. 注入后台地址、兜底 apps、黑名单和 settings。
4. 同步读取上次远程配置缓存。
5. 应用配置并绑定离线包调度器。
6. 拉取 TOP 常驻离线包。
7. 异步刷新 `/api/config`，并启动配置轮询。

配置轮询线上配置为 60 秒，最小 30 秒；SDK 未收到后台覆盖值时默认 300 秒，传 `configRefreshSec: 0` 可关闭。刷新成功后会重新检查 TOP 包、当前地区包以及本会话已经打开过的页面。

主要公开 API：

```text
init / refreshConfig / cancelConfigRefresh / restartConfigRefresh / getConfigRefreshState
getConfigServer / getApps / setApps
loadBundle / loadBundleForApp / loadTopBundles
switchRegion / deleteRegion / deleteBundle
handleSchemeRequest / getConfigJson / updateConfigJson
matchesRegion / getAppRegions
bundleProgress / isBundleApplied / stats / setDebug
```

### 4.2 远程配置

[`app/webaccel/src/main/ets/core/RemoteConfig.ets`](app/webaccel/src/main/ets/core/RemoteConfig.ets)

- 配置来源依次是：直供兜底、沙箱缓存、服务器最新配置。
- 成功拉取服务器配置后写入 `remote-config.json`。
- `refreshConfig()` 返回 `ConfigRefreshResult`，并发调用复用同一任务；取消会销毁真实 HTTP request，requestId 会阻止旧任务写配置、缓存或触发离线包下载。
- 服务器配置会覆盖兜底列表，但会按 `id/url` 从直供列表补齐旧后台缺失的 `scope/region/regions/appId/appVersion/priority`。
- 只有 `localOnly: true` 的直供应用会在服务器列表缺失时继续追加展示。
- 多地区判断必须调用 `WebAccel.matchesRegion(app, region)`，不要直接比较 `app.region`。

### 4.3 离线包调度

[`app/webaccel/src/main/ets/core/WebPreRender.ets`](app/webaccel/src/main/ets/core/WebPreRender.ets)

文件名保留了历史名称，但当前不做预渲染，只负责 Web 节点和离线包调度。

当前调度规则：

- `scope: "top"`：SDK 初始化和配置刷新时自动下载，切区不删除。
- `scope: "region"`：宿主调用 `WebAccel.switchRegion(regionId)` 后，下载 `regions` 包含该 ID 的应用；切区会取消旧任务并删除旧地区缓存。
- `scope: "app"` 或 SDK 中缺少 scope 的直供配置：打开/显式加载对应页面时按需下载。

重要差异：配套后台 [`admin/server/index.js`](admin/server/index.js) 当前把所有非 `region` 的 scope 归一化为 `top`。因此服务器下发实际上只有 `top/region`；`app` 目前主要是 SDK 兼容和直供能力。若要在后台正式配置 `app`，必须先修改后台归一化和管理界面。

### 4.4 缓存、压缩与命中

[`app/webaccel/src/main/ets/core/WebCacheManager.ets`](app/webaccel/src/main/ets/core/WebCacheManager.ets)

关键状态文件：

```text
filesDir/webcache/index.json          # URL -> 本地文件索引
filesDir/webcache/bundle-state.json   # app/bundle 版本、URL 清单、地区归属、configJson
filesDir/webcache/*.bin               # 实际资源字节
```

关键行为：

- 只有 `source === "bundle"` 的磁盘项允许命中；旧 runtime 来源会被移除。
- GET 请求才参与离线命中，其他方法直接走网络。
- 缓存 key 是网页真实 URL，不是 `/bundles/...` 地址。
- 压缩项保存 `.zz` 字节，端侧通过 zlib 解压到内存后返回原文。
- 离线包版本不变时不重新拉 manifest；版本变化时重新比较并清理旧 URL。
- 磁盘超过 `diskCapMB` 后按 LRU 淘汰。
- 资源文件丢失时回退网络，并向 `/api/report-error` 触发后台自愈重建。

外部宿主接入要注意：

- `WebAccelView` 使用 `onInterceptRequest`，会执行 SDK 黑名单短路和离线命中。
- `WebAccel.handleSchemeRequest()` 当前只负责离线资源命中/未命中，不执行 `blockHosts` 黑名单逻辑。
- 使用自定义 SchemeHandler 的宿主如需要黑名单能力，必须单独核对其请求处理链，不能默认与 `WebAccelView` 完全等价。

### 4.5 SDK 自带 UI

- [`WebShared.ets`](app/webaccel/src/main/ets/core/WebShared.ets)：统一 Web 配置；保持 `CacheMode.Default`，FCP 或进度 85% 后收起占位，不注入脚本改网页 DOM。
- [`WebAccelView.ets`](app/webaccel/src/main/ets/WebAccelView.ets)：网页容器、首屏占位和离线包进度条。
- [`WebAccelLauncher.ets`](app/webaccel/src/main/ets/WebAccelLauncher.ets)：应用列表、导航和返回。
- [`WebAccelDebugBadge.ets`](app/webaccel/src/main/ets/WebAccelDebugBadge.ets)：配置来源、命中统计和离线包进度。

## 5. Demo 元服务

[`app/entry/src/main/ets/entryability/EntryAbility.ets`](app/entry/src/main/ets/entryability/EntryAbility.ets)

- `entry` 是元服务 Demo，不是 SDK 的真实业务宿主。
- 当前通过 `WebAccel.init(this.context, { apps: FALLBACK_APPS })` 初始化；后台地址来自 `webaccel/build-profile.json5`。
- Demo 内有较大的兜底应用列表，主要用于本地/远程配置兼容测试。
- 当前 Demo 写死 `WebAccel.setDebug(true)`；正式宿主应按构建类型关闭 release 调试。
- `module.json5` 声明 INTERNET 和定位权限，并设置 `installationFree: true`。
- 元服务单包必须不超过 2 MB，总包不超过 10 MB。

入口页 [`app/entry/src/main/ets/pages/Index.ets`](app/entry/src/main/ets/pages/Index.ets) 只挂载 `WebAccelLauncher()`。

## 6. 真实宿主 AtomicPlatform

`harm-youhua` 与 `D:\Work\KCP\离线包sdk\AtomicPlatform` 是同一交付链路。涉及 SDK API、manifest、地区、SchemeHandler、configJson 或缓存行为时，必须同时验证真实宿主。

工作区选择必须遵守 `D:\Work\KCP\离线包sdk\工作区使用约定.md`。旧目录 `D:\Work\KCP\AtomicPlatform` 不再用于本项目的修改、构建或验证。

宿主维护入口：

```text
D:\Work\KCP\离线包sdk\AtomicPlatform\performance\h5_webaccel\README.md
```

关键链路：

1. `AtomicPlatform/entry/.../EntryAbility.ets`
   - 启动时传入 `BuildProfile.webAccelConfigServer` 初始化 WebAccel；测试/预发/正式分别使用 `/test`、`/pre`、`/prod`。
   - 监听地区变化并调用 `WebAccel.switchRegion()`。
   - 调试日志和浮窗只在 Debug 构建开启。
2. `performance/h5_webaccel/.../WebAccelAppResolver.ets`
   - 集中匹配业务 appCoding、业务 appId、URL、subUrl 和 routes。
3. `performance/h5_webaccel/.../WebAccelBundleLoader.ets`
   - 判断有效压缩版本，并做进程级 30 秒重复调度保护。
4. `performance/h5_webaccel/.../WebAccelWebHandler.ets`
   - 将离线命中接入宿主 HTTPS SchemeHandler，并输出会话级诊断。
5. `flushu/lushu_appcommon_web/.../WebAccelController.ets`
   - 页面侧匹配应用、检查地区、加载离线包、安装请求 handler。
6. `flushu/lushu_appcommon_web/.../CommonWebSDKModel.ets`
   - 优先使用 SDK/远程配置中的 `configJson`；缺失时回源业务接口，并将新 JSON 回写 SDK 作为兜底。

宿主保留自己的 H5 容器、JSBridge、代理、返回逻辑和网络监控；WebAccel 只插入离线资源能力，不替换整套容器。

## 7. 后台服务

### 7.1 Express 服务

[`admin/server/index.js`](admin/server/index.js)

主要职责：

- 公开兼容 `/api/config`、环境接口 `/:environment/api/config` 和共享 `/bundles/*`。
- 登录、会话 token 和后台管理 API。
- 规范化 `scope/regions/configJsonEnvironments/configJsonSync`；旧配置迁移时只写入 test。
- 离线包构建、导入、更新、资源开关和状态查询。
- 每天一次离线包自动检查；服务启动 1 分钟后先执行一轮。
- 每天 12:00（Asia/Shanghai）同步第三方后台的 `configJson`。
- 端侧资源损坏上报后的离线包自愈。
- 可选 MySQL 众包共识数据；数据库不可用不影响配置和离线包主链路。

`GET /api/config` 只在 app 配置了 `bundle:true` 且服务器已有 manifest 时，才向端侧下发有效 `bundle:true`、manifest URL 和内容指纹版本；否则下发 `bundleConfigured:true`、`bundle:false`。

### 7.2 离线包构建器

[`admin/server/cache-builder.js`](admin/server/cache-builder.js)

支持四种主要模式：

- `check`：发现源站资源并与现有 manifest 比较。
- `update`：增量下载新增/缺失资源。
- `build`：在临时目录全量构建，成功后原子替换旧包。
- `manifest`：根据已有缓存文件重新生成清单。

资源来源：

- 自动发现同源 Next.js/CRA/Vite/AEM 的稳定 JS、CSS、字体。
- `bundleExtraUrls` 手工固定资源，可跨域。
- `bundleExcludeUrls` 明确排除资源。
- `staticCache` 当前被后台用于离线包筛选策略，不表示端侧运行时缓存。
- `configJson` 不再写进共享 manifest 或离线资源；它只从环境配置接口下发，避免一个环境污染其他环境。

压缩：

- 原始清单：`manifest.json`。
- 压缩清单：`manifest.zz.json`。
- 可压缩文本使用 `zlib.deflate` 生成 `.zz`，新 SDK 优先下载压缩清单。
- 包预算按压缩后的预计落盘体积控制，默认 `bundleMaxSizeKB=5120`。

### 7.3 管理界面

`admin/web` 使用 Vue3、Vite、Element Plus、Pinia 和 Axios，页面包括：

```text
Dashboard / Apps / Blocklist / Bundles / Settings
```

开发端口 5174，代理后端 8787；生产镜像中由 Express 同源托管构建产物。

## 8. 线上数据与部署红线

线上地址：

```text
测试：https://maidun.chujingservice.com/test
预发：https://maidun.chujingservice.com/pre
正式：https://maidun.chujingservice.com/prod
```

必须牢记：

- 线上有效 `config.json` 和离线包在 Docker 数据卷中，不在 Git 工作树中。
- 不要把本地 `admin/server/data/config.json` 当生产基准，也不要以为提交它会覆盖线上。
- 修改线上配置前先取当前生效配置，备份后整份替换，最后重新请求 `/api/config` 复验。
- 中文应用名不要经错误编码的 PowerShell/curl 管道写入，否则可能变成 `????`。
- 后台凭据在仓库外；用户要求的本机记录只存在父目录 `环境地址与账号（本地敏感信息）.md`，禁止提交 Git 或写入 HAR/HAP。
- `git push server main` 会触发服务器自动构建部署；普通阅读、排查或本地改动不授权推送和部署。

容器：

```text
youhua-admin   # Node + 管理界面
youhua-mysql   # 众包 votes，可选依赖
```

## 9. 本地运行与构建

后台：

```powershell
cd D:\Work\KCP\离线包sdk\harm-youhua\admin\server
npm install
npm start

cd D:\Work\KCP\离线包sdk\harm-youhua\admin\web
npm install
npm run dev
```

HAR：

```powershell
cd D:\Work\KCP\离线包sdk\harm-youhua\app
$env:DEVECO_SDK_HOME='D:\Huawei\DevEco Studio\sdk'
& 'D:\Huawei\DevEco Studio\tools\hvigor\bin\hvigorw.bat' assembleHar -p module=webaccel@default
```

HAP：

```powershell
& 'D:\Huawei\DevEco Studio\tools\hvigor\bin\hvigorw.bat' --mode module -p module=entry@default -p product=default assembleHap
```

产物：

```text
app/webaccel/build/default/outputs/default/webaccel.har
app/entry/build/default/outputs/default/entry-default-signed.hap
```

本机签名、bundleName、hvigor/modelVersion 等临时适配不得提交。正式发布必须使用项目自己的元服务签名和正式 bundleName。

## 10. 验证方式

后台已有 `admin/server/test/config-environments.test.js` 覆盖环境迁移、公开字段隔离、遮罩合并和共享 bundle 路由。端侧生命周期仍需构建与真机验证，不能只看编译通过。

最低验证矩阵：

1. HAR/HAP 构建通过，元服务包体未超限。
2. 首装沙箱为空时能下载目标离线包。
3. 等待 `包完成 x/y` 后，同 URL 出现本地命中。
4. 未进入 manifest 的资源正常回退网络。
5. 压缩资源解压后 JS/CSS/字体可正常使用。
6. TOP 包切区后保留。
7. 地区包切区后取消旧下载、清旧缓存、加载新地区。
8. 后台版本变化后，重启或 `refreshConfig()` 能下载新资源并删除旧 URL。
9. 测试、预发、正式 product 分别请求 `/test`、`/pre`、`/prod`，同站 configJson 可以不同，离线资源保持相同。
10. 清应用数据/重装后可重新下载，不应导致页面无法打开。
11. 在 `AtomicPlatform` 中验证 SchemeHandler、JSBridge、代理、返回键、登录态和 NetMonitor 均未回归。

日志重点：

```text
bundle STORE ...             # 后台离线包写入沙箱
offline HIT ...              # 本地命中
cache STORE ... runtime      # 当前版本不应出现
```

## 11. 已知文档/实现偏差

新会话不要忽略以下差异：

1. `新会话快速上下文.md`、`CLAUDE.md`、部分架构文档仍写“服务端配置到达后下载所有 bundle:true 站点”。当前代码已经改为 TOP/地区/按需调度。
2. `TROUBLESHOOTING.md` 中 RWS 的 `bundle:false + swrDoc + prerender` 是旧方案，不能作为当前定版口径。
3. 后台默认配置和部分注释仍保留 `swrDoc/prerender/prefetchChunks` 历史描述；端侧当前不实现这些能力。
4. SDK README 记录了 `scope=app`，但后台现在只输出 `top/region`；这是能力与配套管理端尚未完全对齐的地方。
5. 历史文档要求端云同步修改 `isHashedAsset/isVersionQuery`；当前这两个函数只存在于后台 `cache-builder.js`，端侧 `WebCacheManager.ets` 已不再做运行时资源筛选。
6. `DEPLOY.md` 仍包含早期 HTTP/旧路径说明；当前生产配置地址以 HTTPS 的 `maidun.chujingservice.com` 为准。

## 12. 修改时不可破坏的规则

- 不恢复运行时自动缓存、SWR、预渲染或 chunk 预取，除非需求明确改变定版范围。
- 不缓存普通 HTML 主文档；带会话/表单状态的 HTML 曾导致业务错误。
- 不向网页注入模糊 DOM 隐藏脚本。
- 保持 `CacheMode.Default`。
- 不把离线资源打进 rawfile/HAP。
- 不硬编码生产应用列表和黑名单；生产数据由后台下发。
- 不把 MP4、大视频、动态 API、埋点、广告或随机会话 URL 放入离线包。
- 改 URL 归一化、manifest 格式、压缩字段、scope/regions 或 configJson 时，同时检查 SDK、admin 和 AtomicPlatform。
- 改 SDK 后重新构建 HAR，并更新 `_产物交付` 中的交付包；只改源码不更新 HAR 不算完成交付。
- `离线包sdk\AtomicPlatform` 当前只做本地接入验证，按用户要求保留未提交状态，不 commit/push；正式接入交给 App 团队。
- 保留用户已有工作区改动；当前阅读时 `.deploy_tmp/` 是未跟踪目录，不要擅自删除或提交。

## 13. 新会话开始工作前检查

```powershell
cd D:\Work\KCP\离线包sdk\harm-youhua
git status --short
git branch --show-current
git log -1 --oneline
```

然后按任务范围选择：

- SDK/缓存/manifest：同时读 `WebAccel.ets`、`WebPreRender.ets`、`WebCacheManager.ets`、`RemoteConfig.ets` 和 AtomicPlatform 宿主。
- 后台配置/API：读 `admin/server/index.js`，不要直接拿本地 config 当线上数据。
- 离线包资源选择/压缩：读 `cache-builder.js` 和线上目标站当前 manifest。
- 管理界面：读 `admin/web/src/stores/config.js`、`views/Apps.vue`、`views/Bundles.vue`。
- 构建/真机：先确认 DevEco、签名、HDC 和设备状态，不提交本机临时适配。

只做阅读和诊断时，不修改线上配置、不部署、不推送；需要修改时，完成代码、构建/验证和交付物同步后再交接。
## 14. 2026-08-17 最终三环境规则

- test/pre/prod 的 `configJson` 与离线包都分开管理、分开存储、分开下发；只共用基础应用信息和地区 ID。
- 地址分别为 `https://maidun.chujingservice.com/test|pre|prod`，兼容根地址默认 prod。
- 服务端目录为 `bundles/test`、`bundles/pre`、`bundles/prod`。
- 正式环境保留原有 16 个包；本轮新增 10 个应用全部归 test（包含 MDAC）；pre 当前 0。正式环境不得下发这 10 个测试应用。
- HAR 成功应用配置后会通过 `reconcileConfiguredBundles` 删除当前环境未启用的历史包，避免正式用户继续命中测试资源。
- 最终 HAR SHA-256：`56DAF3162EDF6272BF3DA86E4EF1D1C678C9733840E6111812907DF56F9C1243`。
- AtomicPlatform 的 `atomicservice_h5_webaccel` 是 HSP；替换 HAR 后必须刷新 ohpm/Hvigor 缓存并同时重建、安装 HSP 与 HAP。
- 后续离线包不放图片，只维护稳定 CSS、JS、字体；未知 URL 仍需真机监控后人工录入对应环境。
- 完整交付状态、地址账号和真机证据分别见上级目录的 `当前实施状态.md`、`环境地址与账号（本地敏感信息）.md`、`华为真机离线包问题记录-2026-08-17.md`。
