# 鸿蒙网页应用加速方案说明

> 本文档说明 harm-youhua 项目"是什么方案、用什么方式做到秒开"。基于真机(HUAWEI HBP-AL00 / HarmonyOS 6.0)实测数据。
> 更新:2026-06-17

---

## 一、一句话概括

把一批**网页应用**(K11、Booking、印尼入境卡…)包进一个**鸿蒙元服务(免安装)**,通过**多层加速**做到"点开秒显",并配一个**后台**让所有应用、加速策略、离线包都能**远程下发、改东西不用重新发版**。

```
用户点图标 → 元服务(免安装,装机包仅 412KB)→ 多层加速的 ArkWeb 网页 → 秒开
                          ↑ 开机拉配置 / 按需下离线包
                    后台(配置 + 离线包托管,改完即生效)
```

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────┐
│  端侧:鸿蒙元服务(装在用户手机上)                          │
│                                                          │
│   entry (HAP, 元服务壳, 412KB)                            │
│      └─ 几行代码 WebAccel.init()                          │
│   webaccel (HAR, 加速能力 SDK ← 所有加速逻辑在这)          │
│      ├─ 离屏预渲染池   ├─ 离线缓存/拦截                     │
│      ├─ 主文档 SWR     ├─ 路由预取                         │
│      └─ 黑名单拦遥测                                       │
└──────────────┬──────────────────────────────────────────┘
               │ 开机拉 /api/config、按需下 /bundles/...
               ▼
┌─────────────────────────────────────────────────────────┐
│  后台:Node + Express(配置 API + 离线包托管)+ Vue 管理界面 │
│   ├─ /api/config         下发应用列表/黑名单/加速参数       │
│   ├─ /bundles/<id>/...    离线包托管                       │
│   └─ cache-builder       抓站点资源打离线包                 │
└─────────────────────────────────────────────────────────┘
```

**两层模块的意义:**
- **webaccel(HAR)**= 加速能力 SDK,**一套**逻辑所有应用共用。上架几十个应用不用重写加速。
- **entry(HAP)**= 元服务本体,几行代码消费 SDK。装到设备上跑的就是它。

---

## 三、核心:用了哪几种加速方式

加速是**多层叠加**的,不是单一手段。每一层解决一类问题:

### 1. 离屏预渲染(Prerender)—— 秒开的主力
- **做什么**:开机时,在屏幕外(用户看不到的地方)就提前把网页用 `BuilderNode` 建好、开始加载渲染。用户点图标时,直接把这个"已经渲染好的页面"挂上来。
- **效果**:点击 → 立即显示,**没有白屏、没有 loading 转圈**。
- **优化**:只优先预渲染前 2 个常用站(`MAX_EAGER_PRERENDER=2`),其余延迟分批,避免多站同时建页面互抢资源。

### 2. 离线包(Bundle)—— 静态资源提前到本地
- **做什么**:后台 `cache-builder` 提前把网页的静态资源(JS/CSS/字体)抓下来存服务器;设备开机按需下载进沙箱。打开网页时,这些资源**从本地秒取**,不用去原站慢慢拉。
- **谁能用**:同源静态资源能打包。印尼入境卡(38个资源)、**K11(4个资源1.6MB,新做的)**都有离线包。
- **Booking** 没法打包(反爬+资源全在跨域CDN),靠下面的运行时缓存覆盖。

### 3. 主文档 SWR(Stale-While-Revalidate)—— 重开秒出
- **做什么**:缓存上次加载过的网页主文档(HTML)。重新打开时**先用缓存的旧版秒显**,后台同时校验有没有新版,有新版下次自动换。
- **效果**:重开应用秒出内容,不等网络。

### 4. 路由预取(新做的)—— 治"点二级页慢"
- **做什么**:对配了 `routes` 的站(如 K11 的 `/zh-cn/shop/`、`/zh-cn/taste/`),开机就把这些二级页的**主文档**提前拉到缓存。点"美食页"时主文档已在本地。
- **边界**:只缓主文档;二级页的动态数据(餐厅列表等接口)仍实时拉,那是网站后端的事,框架管不了。

### 5. 黑名单拦遥测(Blocklist)—— 不在无用请求上挂起
- **做什么**:网页里那些境内打不通的遥测/追踪请求(Google分析、广告、Booking的`otel-gw`/`sink.gw`等),在 WebView 层直接拦掉(返回空响应),不让它们卡住页面。
- **效果**:实测拦掉 12+ 个遥测请求,页面不在这些超时请求上空等。

### 6. chunk 预取 / 运行时缓存 —— 边用边缓 + 二级资源
- chunk 预取:对 Next.js 站预取后续页面的 JS(K11非Next站,这条对它不生效,改用上面的路由预取)。
- 运行时缓存:`onInterceptRequest` 在 WebView 层拦截**所有**请求(含跨域),把能缓的静态资源缓到本地,第二次访问就快。Booking 的资源主要靠这个。

---

## 四、实测效果(本台真机)

### 速度
| 指标 | 数值 |
|---|---|
| **元服务冷启动** | **约 0.43 秒**(420-446ms,5轮稳定) |
| **印尼入境卡首屏** | **136ms**(离线包命中,最快) |
| **K11 首屏** | ~410ms(现有离线包后更快) |
| **Booking 首屏** | ~1100ms(源站+跨境代理慢,是天花板) |
| **点进应用/翻二级页** | **秒开**,预渲染池命中,无 loading 卡顿 |

### 存储
| 项目 | 大小 |
|---|---|
| 元服务装机包(HAP) | **412 KB**(极小) |
| 后端离线包 | 印尼入境卡 1.8MB + K11 1.6MB |
| 真机沙箱占用 | 约 16MB / 164条(64MB上限 + LRU 自动淘汰) |

---

## 五、三个应用的加速策略对比

| | K11 | Booking | 印尼入境卡 |
|---|---|---|---|
| 站点类型 | 动态站 | 动态+重反爬 | 政府站(较静态) |
| 离屏预渲染 | ✅ | ✅ | ✅ |
| 主文档 SWR | ✅ | ✅ | ✅ |
| **离线包** | ✅ **(新做,1.6MB)** | ❌ 反爬+跨域,做不了 | ✅(1.8MB) |
| 路由预取 | ✅ /shop/ /taste/ | ✅ | ✅ |
| 黑名单 | 通用 | +Booking遥测域 | 通用 |
| 运行时缓存 | ✅(跨域图片) | ✅(主力,bstatic资源) | ✅ |
| 首屏实测 | ~410ms | ~1100ms(天花板) | 136ms |

---

## 六、框架能优化的 vs 管不了的(诚实边界)

**框架能做的(已做):**
- ✅ 静态资源缓存/离线(JS/CSS/字体/图片)
- ✅ 离屏预渲染、主文档缓存、路由预取
- ✅ 拦掉无用遥测请求

**框架管不了的(天花板,需如实说明):**
- ❌ 网站后端响应慢(如政府源站、Booking 的 SSR)
- ❌ 动态接口数据(实时拉的餐厅列表、酒店搜索结果)
- ❌ 网站自己的 JS 执行速度
- ❌ 跨境网络/代理延迟

> 一句话:**我们让"加载资源"尽量快尽量本地化;接口数据和 JS 执行是网站自己的事,管不了。** Booking 慢主要慢在它后端+跨境,框架已尽力。

---

## 七、运营方式:上架一个新应用要做什么

**核心优势:改东西不用重新发版 App。**

1. 后台管理界面(admin/admin123)→ 新增应用,填名称/URL/加速开关 → 保存
2. (可选)点"构建"给它建离线包
3. 设备下次开机拉 `/api/config` 自动出现新应用、自动下离线包

**端侧代码不用动、不用重新发版。** 这就是支撑"上架几十个应用"的关键——加速能力是一套(webaccel HAR),应用全靠后台配置驱动。

---

## 八、当前状态

- ✅ 三个应用都已真机跑通、秒开
- ✅ K11 离线包新做完成(版本号 query 资源也能离线了,通用能力)
- ✅ 后台配置驱动,可纯后台上架新应用
- 可演示状态
