# harm-youhua 鸿蒙网页加速 — 优化报告

> 报告日期:2026-06-18 ｜ 测试设备:HUAWEI HBP-AL00（HarmonyOS 6.0，API 22）真机
> 测试应用:K11 香港、Booking.com、印尼入境卡（印尼海关 e-CD）

---

## 一、本次优化目标与结论

| 目标 | 结论 |
|---|---|
| **更快**（秒点秒开） | ✅ 达成。冷启动 0.43s，点进应用/翻页预渲染秒开，印尼入境卡首屏 136ms |
| **更小**（包体/离线包） | ✅ 装机包仅 435KB；离线包剔除 sourcemap |
| **管理后台更好用** | ✅ 一键探测上架、离线包状态一览、一键建包 |
| **每应用精细配置** | ✅ 端云闭环：每应用可单独配额外黑名单、预取开关 |

**一句话**:端侧"加载体验"已优化到位（秒开），后台从"纯手工"升级为"智能探测 + 每应用精细管理"，为上架几十个应用铺好路。

---

## 二、性能实测数据（真机，对比 B 标场景）

### 冷启动（5 轮稳定）
| 指标 | 数值 |
|---|---|
| **元服务冷启动 e2e** | **434ms**（区间 420–446ms） |

### 三应用首屏（用户看到内容的时间 FCP）
| 应用 | 优化后首屏 | 说明 |
|---|---|---|
| **印尼入境卡** | **~136ms** | 离线包命中，最快 |
| **K11** | **~410ms** | 新增离线包后首屏关键资源本地化 |
| **Booking** | **~1100ms** | 源站 SSR + 跨境代理慢，是天花板 |

### 操作体验
- 点进应用、翻二级页（K11 店铺/美食页）：**预渲染池命中，立即显示，无白屏无 loading 卡顿**。

---

## 三、用了哪些加速手段（技术方案）

加速是**多层叠加**，每层解决一类问题：

| # | 手段 | 作用 | 本次改动 |
|---|---|---|---|
| 1 | **离屏预渲染** | 开机在屏幕外预先把网页渲染好，点击直接挂上来 → 秒开 | 加优先级：只热预前 2 个常用站，避免互抢资源 |
| 2 | **离线包** | 提前把网页静态资源（JS/CSS/字体）抓到服务器，设备下载到本地秒取 | **新增 K11 离线包**（让带版本号 query 的资源也能缓，见下） |
| 3 | **主文档 SWR** | 缓存上次的网页文档，重开先用旧版秒显、后台校验更新 | 沿用 |
| 4 | **路由预取** | 对配了 routes 的页面，预先下载它引用的静态资源 | **新增**：只下资源、不缓动态页主文档（避免出错） |
| 5 | **黑名单拦遥测** | 拦掉境内打不通的遥测/广告请求，不让它们卡住页面 | 升级为"全局 + 每应用"两级 |
| 6 | **运行时缓存** | WebView 层拦截所有请求（含跨域），能缓的边用边缓 | 沿用 |

### 关键技术突破:版本号 query 资源可离线（让 K11 能建离线包）
- **问题**:K11 的核心 JS/CSS（约 1.6MB，同源）命名是 `bundle.min.js?2025121805`（query 版本号，非内容 hash），原逻辑判为不可缓存而漏掉，导致 K11 没法建离线包。
- **方案**:让端云两侧都识别"版本号型 query"（纯数字，如 `?2025121805`）为可缓存（版本变即新 URL，等效 hash），严格排除随机参数（`?t=时间戳`/`?token=xxx`）防缓存爆炸。
- **结果**:K11 离线包 4 个资源 1.6MB，真机浮窗显示"K11 包 4/4"。**这是通用能力**，后面任何用版本号 query 的站都受益。

---

## 四、管理后台升级（为上架几十个应用）

### 新增能力
| 功能 | 说明 |
|---|---|
| **一键探测** | 新增应用填 URL → 点"一键探测" → 自动填入网站名称、判断站点类型（Next/Vite/CRA/其他）、给出建议加速参数（哪些开关该开），免人工判断 |
| **离线包状态列** | 应用列表直接显示每个应用的离线包"已建（N个/XKB）/待构建/不启用"，一眼看清 |
| **一键建离线包** | 新增应用开了离线包但没建时，保存后弹窗提示立即构建 |
| **每应用额外黑名单** | 编辑应用时可填"只对本应用生效的屏蔽域"（如 Booking 专属遥测），叠加在全局黑名单上 |
| **每应用预取开关** | 每个应用可单独控制 chunk 预取 |

### 上架一个新应用的流程（现在）
```
后台填 URL → 一键探测（自动名称/类型/建议参数）
   → 保存 → 弹窗一键建离线包
   → 设备下次开机自动出现新应用、自动下离线包
全程不改代码、不重新发版 App
```

---

## 五、框架能优化的 vs 管不了的（诚实边界）

**框架能做的（已做）:**
- ✅ 静态资源缓存/离线（JS/CSS/字体/图片）
- ✅ 离屏预渲染、主文档缓存、路由预取
- ✅ 拦掉无用遥测请求

**框架管不了的（天花板，需如实说明）:**
- ❌ 网站后端响应慢（Booking 的 SSR、印尼政府源站）
- ❌ 动态接口数据（实时拉的酒店搜索结果、表单下拉选项）
- ❌ 网站自己的 JS 执行速度
- ❌ 跨境网络/代理延迟

### 排查实例:印尼入境卡表单页 "Reference error"
- **现象**:印尼入境卡 BC32/BC34 表单页报 Reference error。
- **排查**:真机日志确认这些接口（`/api/reference/negara` 等）我们**没缓、没拦、透传走网络**；命令行直接 curl 源站 → `negara` 接口**返回 401**（源站自己报错）。
- **结论**:**是印尼海关原网站接口的问题（401），与加速框架无关**，框架碰不了。

---

## 六、存储与包体

| 项目 | 大小 |
|---|---|
| 元服务装机包（HAP） | **435 KB**（极小，远低于元服务 2MB 上限） |
| 后端离线包 | 印尼入境卡 1.8MB + K11 1.6MB |
| 真机沙箱占用 | 约 16MB（64MB 上限 + LRU 自动淘汰，不会无限涨） |

---

## 七、三应用加速策略一览

| | K11 | Booking | 印尼入境卡 |
|---|---|---|---|
| 站点类型 | 动态站 | 动态+重反爬 | 政府站 |
| 离屏预渲染 | ✅ | ✅ | ✅ |
| 主文档 SWR | ✅ | ✅ | ✅ |
| **离线包** | ✅ 1.6MB（新做） | ❌ 反爬+跨域做不了 | ✅ 1.8MB |
| 路由预取 | ✅ | ✅ | ✅ |
| 每应用黑名单 | — | ✅ 遥测域 | — |
| 运行时缓存 | ✅ 跨域图片 | ✅ 主力 | ✅ |
| **首屏实测** | ~410ms | ~1100ms(天花板) | 136ms |

---

## 八、结论

1. **端侧加载体验已优化到位**:冷启动 0.43s、秒点秒开，继续抠端侧代码收益很小、且有风险（实践中验证过激进改动会引入副作用）。
2. **真正瓶颈在网站后端和网络**（源站慢、跨境、动态接口），这部分框架碰不了，已如实标注。
3. **后台已支持"智能探测 + 每应用精细管理"**，上架新应用从"手填一堆开关"压到"填 URL 点两下"，且每应用能单独精细配置，**支撑上架几十个应用**。
4. **当前为完整可用、真机验证过的状态。**
